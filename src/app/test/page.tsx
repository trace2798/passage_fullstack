import { getPosyByUserId } from "@/actions/getPostByUserId";
import { FC } from "react";
import { Passage } from "@passageidentity/passage-js";

interface pageProps {}

const page: FC<pageProps> = async ({}) => {
  return (
    <>
      <div>page</div>
    </>
  );
};

export default page;
